<div class="field">
  <label for="<%= id %>">
    <%= label %>
  </label>
  <% if local_assigns[:description] %>
    <p> <%= description %> </p>
  <% end %>
  <%= form.select name, data, id: id %>
</div>