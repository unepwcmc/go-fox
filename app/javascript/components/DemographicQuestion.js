import React from "react"
import PropTypes from "prop-types"

export default class DemographicQuestion extends React.Component {
  // Will take in a demographic question, and render appropriate sub component

  render () {
    return(
      <div>
      </div>
    );
  }
}
